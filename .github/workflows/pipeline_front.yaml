name: Pipeline API

on:
  push:
    branches:
      - develop
      - main
    paths:
      - 'front/**'
  pull_request:
    paths:
      - 'front/**'

jobs:
  pipeline-api:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create docker network
        run: make network

      - name: Docker up
        run: make up front

      - name: Vendor
        run: make front-vendor

      - name: Linter
        run: make front-linter
